---
type OverlayProps = {
  isRes: boolean;
  extraClass: string;
};

const { isRes = false, extraClass } = Astro.props as OverlayProps;
---

<div
  class={`${extraClass} hidden fixed opacity-70 z-20 h-screen w-screen bg-black block ${
    isRes ? 'xl:hidden' : ''
  } `}
>
</div>
